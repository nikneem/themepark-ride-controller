## 1. Project Setup

- [x] 1.1 Create `src/ThemePark.Shared/ThemePark.Shared.csproj` as a `Microsoft.NET.Sdk` class library targeting `net10.0` with no NuGet package references
- [x] 1.2 Add `ThemePark.Shared` to the solution file (`ThemePark.sln`) so it participates in `dotnet build` and `dotnet test`
- [x] 1.3 Remove the placeholder `Class1.cs` from `ThemePark.Shared` (if generated by the SDK template)

## 2. Enum Definitions

- [x] 2.1 Create `src/ThemePark.Shared/Enums/RideStatus.cs` defining `RideStatus` with values: `Idle`, `PreFlight`, `Loading`, `Running`, `Paused`, `Maintenance`, `Resuming`, `Completed`, `Failed`
- [x] 2.2 Create `src/ThemePark.Shared/Enums/ChaosEventType.cs` defining `ChaosEventType` with values: `WeatherAlert`, `MascotIntrusion`, `MechanicalFailure`
- [x] 2.3 Create `src/ThemePark.Shared/Enums/WeatherSeverity.cs` defining `WeatherSeverity` with values: `Calm`, `Mild`, `Severe`
- [x] 2.4 Create `src/ThemePark.Shared/Enums/MaintenanceStatus.cs` defining `MaintenanceStatus` with values: `Pending`, `InProgress`, `Completed`, `Cancelled`
- [x] 2.5 Create `src/ThemePark.Shared/Enums/MaintenanceReason.cs` defining `MaintenanceReason` with values: `MechanicalFailure`, `ScheduledCheck`, `Failure`
- [x] 2.6 Create `src/ThemePark.Shared/Enums/RefundReason.cs` defining `RefundReason` with values: `MechanicalFailure`, `WeatherClosure`, `OperationalDecision`
- [x] 2.7 Create `src/ThemePark.Shared/Enums/ChaosEventResolution.cs` defining `ChaosEventResolution` with values: `MascotCleared`, `WeatherCleared`, `SafetyOverride`

## 3. Record Definitions

- [x] 3.1 Create `src/ThemePark.Shared/Records/Passenger.cs` as `public sealed record Passenger(string PassengerId, string Name, bool IsVip)`
- [x] 3.2 Create `src/ThemePark.Shared/Records/RideInfo.cs` as `public sealed record RideInfo(Guid RideId, string Name, int Capacity, string Zone)`
- [x] 3.3 Create `src/ThemePark.Shared/Records/ChaosEvent.cs` as `public sealed record ChaosEvent(string EventId, ChaosEventType Type, string Severity, DateTimeOffset ReceivedAt, bool Resolved)`

## 4. Integration Event Base

- [x] 4.1 Create `src/ThemePark.Shared/Events/IntegrationEvent.cs` as `public abstract record IntegrationEvent(string EventId, DateTimeOffset OccurredAt)` — the base type all pub/sub event records inherit from

## 5. Ride Seed Data

- [x] 5.1 Create `src/ThemePark.Shared/Catalog/RideCatalog.cs` as a `public static class RideCatalog` with 5 `static readonly RideInfo` fields using hard-coded stable GUIDs:
  - `ThunderMountain` — "Thunder Mountain", capacity 24, Zone-A
  - `SpaceCoaster` — "Space Coaster", capacity 12, Zone-A
  - `SplashCanyon` — "Splash Canyon", capacity 20, Zone-B
  - `HauntedMansion` — "Haunted Mansion", capacity 16, Zone-C
  - `DragonsLair` — "Dragon's Lair", capacity 8, Zone-A
- [x] 5.2 Add `public static readonly IReadOnlyList<RideInfo> All` to `RideCatalog` containing all 5 rides as `Array.AsReadOnly(new[] { ... })`

## 6. Project References

- [x] 6.1 Add `<ProjectReference>` to `ThemePark.Shared` in `ThemePark.ControlCenter` project file
- [x] 6.2 Add `<ProjectReference>` to `ThemePark.Shared` in `ThemePark.Rides` project file
- [x] 6.3 Add `<ProjectReference>` to `ThemePark.Shared` in `ThemePark.Queue` project file
- [x] 6.4 Add `<ProjectReference>` to `ThemePark.Shared` in `ThemePark.Maintenance` project file
- [x] 6.5 Add `<ProjectReference>` to `ThemePark.Shared` in `ThemePark.Weather` project file
- [x] 6.6 Add `<ProjectReference>` to `ThemePark.Shared` in `ThemePark.Mascots` project file
- [x] 6.7 Add `<ProjectReference>` to `ThemePark.Shared` in `ThemePark.Refunds` project file

## 7. Verification Build

- [x] 7.1 Run `dotnet build` from the solution root and confirm zero errors and zero warnings related to `ThemePark.Shared`

## 8. Unit Tests

- [x] 8.1 Create `tests/ThemePark.Shared.Tests/ThemePark.Shared.Tests.csproj` referencing xUnit, Moq (if needed), and `ThemePark.Shared`
- [x] 8.2 Add the test project to the solution file
- [x] 8.3 Write `RideStatusEnumTests` verifying `RideStatus` has exactly 9 values with the correct names
- [x] 8.4 Write `RideCatalogTests` verifying `RideCatalog.All` contains exactly 5 rides, all GUIDs are distinct, and zone assignments match the spec
- [x] 8.5 Write `IntegrationEventTests` verifying a concrete subclass correctly inherits `EventId` and `OccurredAt`
- [x] 8.6 Run `dotnet test` and confirm all tests pass
